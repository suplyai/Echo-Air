<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      BLE runtime permissions. We intentionally do NOT declare neverForLocation
      on BLUETOOTH_SCAN. Honor / MagicOS and Huawei / EMUI (and to a lesser
      extent Xiaomi / MIUI) silently require ACCESS_FINE_LOCATION to be
      granted before BluetoothLeScanner.startScan returns any results, even
      when the app declares neverForLocation. Confirmed via parallel native
      + library scans on an Honor ALI-NX1 with a device advertising at
      -49 dBm: zero hits from either scanner until FINE_LOCATION was granted.
      LatAm pilot hardware will have a large Honor/Huawei/Xiaomi footprint,
      so we pay the UX cost of asking for Location on every version rather
      than ship an app that silently breaks on a large slice of deployments.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!--
      Required for BLE scanning on all Android versions in practice. The app
      itself does not read the user's location — see the rationale string
      that the runtime request surfaces.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Background location for ambient mode on pre-Android-12 builds only -->
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Foreground service for active BLE collection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Notifications for Android 13+ collection foreground notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Platform vibrator for the "confirmed scan" / submit-success haptic tick.
      VIBRATE is a normal permission — no runtime grant required. Used because
      Compose's LocalHapticFeedback wrapper is suppressed by some OEM skins
      (Honor / MagicOS) even when the user expects a firm pulse.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <application
        android:name=".EchoAirApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EchoAir"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        tools:targetApi="33">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.EchoAir">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ble.service.CollectionScanService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>

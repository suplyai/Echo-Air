<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          Debug-only entry point. Launch from the device shell:
            adb shell am start -n app.suply.echoair.debug/app.suply.echoair.spike.SpikeActivity
          Or from the app's Settings screen (debug-only button).
        -->
        <activity
            android:name="app.suply.echoair.spike.SpikeActivity"
            android:exported="true"
            android:label="Echo Air — kbeaconlib2 spike"
            android:theme="@style/Theme.EchoAir">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

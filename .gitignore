*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/app/release
*.apk
*.aab
*.keystore
*.jks
keystore.properties
# Signing material — production key lives here, never committed.
# Anything inside signing/ is treated as secret regardless of extension.
/signing/
# Defensive: the previous (now rotated/abandoned) production key lived
# here. Keep the patterns so an accidental restore from history doesn't
# silently re-commit either file.
/app/echonew_keystore
/app/keystore_details.txt
google-services.json
.kotlin
local.properties
/libs/kbeaconlib2/build/
/libs/kbeaconlib2/.gradle/
app/build/outputs/apk/debug/app-debug.apk
